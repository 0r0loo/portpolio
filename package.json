{
  "name": "myproject",
  "packageManager": "yarn@3.5.0",
  "private": true,
  "scripts": {
    "common": "yarn workspace @blue/common",
    "components": "yarn workspace @blue/components",
    "nextapp": "yarn workspace next-app",
    "nestapp": "yarn workspace nest-app"
  },
  "workspaces": [
    "packages/*/*",
    "packages/libs/frontend/*",
    "packages/libs/backend/*"
  ],
  "devDependencies": {
    "@types/prettier": "^2",
    "prettier": "^2.8.7",
    "typescript": "^5.0.2"
  }
}
